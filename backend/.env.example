# 复制为 .env 后按需修改（不要将含真实密码的 .env 提交到版本库）
#
# ========== Sealos：发布 / 部署（与文档 release + deploy 对齐）==========
# 1. 发版前在 DevBox 终端执行：bash scripts/prep-release-backend.sh
# 2. 确认项目根目录存在可执行的 entrypoint.sh：chmod +x entrypoint.sh
# 3. 控制台 DevBox 详情 → 版本历史 →「发布版本」，填写如 v1.0.0
# 4. 发版成功后对该版本点「上线」，在【应用管理】配置：
#    - 资源配额、对外端口（与容器内 PORT 一致，默认 8000）
#    - 下列环境变量（推荐只在控制台配置，不要打进镜像）
#
# ========== Sealos 内置 PostgreSQL（数据库应用与后端同一集群）==========
# 在 Sealos「数据库」→ 你的 PostgreSQL → 连接信息中查看主机、端口、库名、用户、密码。
# 组装为 SQLAlchemy URL（密码中含特殊字符时请 URL 编码）：
#   postgresql+psycopg2://用户:密码@主机:5432/数据库名
# 集群内主机常为 *.svc 形式（示例见下，请以控制台为准）。
#
ENVIRONMENT=production
DATABASE_URL=postgresql+psycopg2://postgres:CHANGE_ME@bus-system-postgresql.ns-1ht608x0.svc:5432/bus_system_test
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRE_MINUTES=120
# 浏览器访问线上前端的 Origin，逗号分隔（HTTPS）；仅部署 API 时可先填控制台预览域名或后续前端域名
CORS_ORIGINS=https://fhlkzwzoizzu.sealosbja.site
#
# 正式上线关闭演示数据写入
DEMO_SEEDING_ENABLED=false
#
# 首次启动若库中无该用户则自动创建
BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_PASSWORD=Admin123!@#
BOOTSTRAP_ADMIN_DISPLAY_NAME=系统管理员
#
# ========== 本地开发（可选：注释掉上方 Sealos 配置后使用）==========
# ENVIRONMENT=development
# DATABASE_URL=postgresql+psycopg2://bus_test:bus_test_123@127.0.0.1:55432/bus_system_test
# CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
# DEMO_SEEDING_ENABLED=true
