# Vue3 前端（哈尔滨工务段汽车管理信息系统）

## 安装与启动

```powershell
Set-Location "E:\VS_code\project\公车系统\系统复现\frontend"
npm install
npm run dev
```

开发服务器默认 `http://127.0.0.1:5173`，并通过 Vite 代理将 `/api` 转发到 `http://127.0.0.1:8000`。

## 构建

```powershell
npm run build
npm run preview
```
