# 数据库连接说明（复制需要的片段到 backend/.env 或 Sealos 应用环境变量）
# 日常运行请始终使用「集群内网」地址；外网地址仅用于一次性迁移脚本。
#
# 库名均为：bus_system_test
# SQLAlchemy 写法需在 postgresql:// 后加 +psycopg2

# ---------- 开发环境（DevBox / 开发后端 Pod）内网 ----------
# DATABASE_URL=postgresql+psycopg2://postgres:<开发库密码>@bus-system-postgresql.ns-1ht608x0.svc:5432/bus_system_test

# ---------- 生产环境（生产后端 Pod）内网 ----------
# DATABASE_URL=postgresql+psycopg2://postgres:<生产库密码>@test-db-postgresql.ns-1ht608x0.svc:5432/bus_system_test

# ---------- 仅数据迁移时使用（scripts/migrate_dev_db_to_prod.py）外网 ----------
# DEV_DATABASE_URL=postgresql://postgres:<开发库密码>@dbconn.sealosbja.site:39754/bus_system_test
# PROD_DATABASE_URL=postgresql://postgres:<生产库密码>@dbconn.sealosbja.site:48528/bus_system_test
